<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp">

        <!-- Selection Checkbox -->
        <ImageView
        android:id="@+id/imgSelect"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_uncheck_circle_outline"
        android:layout_marginEnd="16dp"
        android:visibility="gone"/> <!-- Visible only in selection mode or when selected -->

        <!-- File/Folder Icon -->
        <RelativeLayout
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="16dp">
            
            <ImageView
        android:id="@+id/imgFileIcon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/ic_folder"
        app:tint="#FFC107"/> <!-- Yellow folder base -->
                
            <!-- Small overlay icon (e.g. camera, download) -->
            <ImageView
        android:id="@+id/imgOverlay"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_image"
        android:tint="#FFFFFF"
        android:visibility="gone"/>
        </RelativeLayout>

        <!-- Title and Date -->
        <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

            <TextView
        android:id="@+id/textFileName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Folder Name"
        android:textColor="@color/md3_text_primary"
        android:textSize="11sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginBottom="4dp"/>

            <TextView
        android:id="@+id/textFileDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="4 Aug 4:30 pm"
        android:textColor="@color/md3_text_secondary"
        android:textSize="11sp"
        android:maxLines="1"/>
        </LinearLayout>

        <!-- Item Count / Size -->
        <TextView
        android:id="@+id/textItemCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 items"
        android:textColor="@color/md3_text_primary"
        android:textSize="11sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginStart="16dp"/>
    </LinearLayout>

    <!-- Thin Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#1AFFFFFF"
        android:layout_marginStart="88dp"
        android:layout_marginEnd="16dp"/>

</LinearLayout>
